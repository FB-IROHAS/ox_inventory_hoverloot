# ox_inventory_hoverloot
hoverloot for ox_inventory

設定はconfig.cfgのこの部分を変更してください。
```lua
### Hover Loot
# The key used for hover looting (Default: H)
setr inventory:hoverkey "H"

# The delay in milliseconds before the item is transferred (Default: 200)
setr inventory:hoverdelay 200
```
